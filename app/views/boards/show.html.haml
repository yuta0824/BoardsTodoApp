.bg-white.shadow-lg.rounded-lg.max-w-2xl.container.p-0.relative.z-2
  %span.absolute.left-12.top-0.bottom-6.w-1.bg-gray-200.-z-10
  .rounded-lg.border.border-gray-400
    = render 'commons/board-card', board: @board, show_read_more: false
  .p-6.space-y-4.relative
    - @tasks.each do |task|
      = render 'commons/task-card', board: @board, task: task, show_read_more: false
    = link_to '＋ Add new Task', new_board_task_path(@board.id), class: 'min-h-30 bg-white flex items-center justify-center font-midium text-lg p-5 rounded-lg shadow-md border border-gray-400 border-dashed'
